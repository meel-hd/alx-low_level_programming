#include "main.h"

/**
 * _sqrt_recursion - returns the natural square root of a number
 * @n: the number
 * Return: the result or 0 if the number
 * does'nt have natural sqaure root
 */
int _sqrt_recursion(int n)
{
	return (-1);
}
