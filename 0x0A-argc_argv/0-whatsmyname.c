#include <stdio.h>

/**
 * main - Entry point
 * @argc: arguments count
 * @argv: cmd line args
 * Return: 0 always
 */
int main(int argc, char *argv[])
{
	(void)argc;

	printf("%s\n", argv[0]);
	return (0);
}
